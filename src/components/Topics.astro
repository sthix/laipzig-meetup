---
import SectionHead from './SectionHead.astro';
import { TOPICS } from '../data/meetup';
---

<section class="section" id="topics">
	<div class="shell">
		<SectionHead
			label="What comes up"
			note="Entries staggered in the order the field invented them"
		/>

		<ul class="voices">
			{
				TOPICS.map((topic, i) => (
					<li class="voice rise" style={`--i: ${i}; --rise-delay: ${i * 70}ms`}>
						<span class="entry" aria-hidden="true" />
						<h3 class="name">{topic.name}</h3>
						<p class="note">{topic.note}</p>
					</li>
				))
			}
		</ul>
	</div>
</section>

<style>
	.section {
		position: relative;
		z-index: 2;
		background: var(--color-ink);
		padding-block: 0 var(--section-gap);
	}

	.voices {
		list-style: none;
		margin: 0;
		padding: 0;
	}

	/* Each voice enters later and further in — a stretto, read top to bottom.
	   The rules that used to separate them are gone: whitespace and the
	   staggered indent do the separating now. */
	.voice {
		position: relative;
		display: grid;
		grid-template-columns: minmax(0, 0.9fr) minmax(0, 1fr);
		gap: clamp(var(--space-2), 4vw, var(--space-6));
		align-items: baseline;
		padding: clamp(var(--space-3), 2.6vw, var(--space-5)) 0;
		padding-left: calc(var(--i) * 2.7%);
		transition: padding-left 0.45s cubic-bezier(0.16, 1, 0.3, 1);
	}

	/* Tick marking where this voice comes in — the only vertical mark left. */
	.entry {
		position: absolute;
		left: calc(var(--i) * 2.7%);
		top: 0;
		width: 1px;
		height: 100%;
		background: var(--color-paper);
		opacity: 0;
		transform: scaleY(0.3);
		transform-origin: top;
		transition:
			opacity 0.35s ease,
			transform 0.45s cubic-bezier(0.16, 1, 0.3, 1);
	}

	.voice:nth-child(3n + 1) .entry {
		background: var(--color-cyan);
	}
	.voice:nth-child(3n + 2) .entry {
		background: var(--color-magenta);
	}
	.voice:nth-child(3n + 3) .entry {
		background: var(--color-yellow);
	}

	/* 400 throughout. Hover opens the width axis, never the weight axis. */
	.name {
		font-family: var(--font-display);
		font-variation-settings: 'wdth' 100, 'wght' 400;
		font-size: var(--text-heading-sm);
		font-weight: 400;
		line-height: 1.02;
		letter-spacing: var(--tracking-display);
		margin: 0;
		transition: font-variation-settings 0.45s cubic-bezier(0.16, 1, 0.3, 1);
	}

	.note {
		margin: 0;
		color: var(--color-paper-3);
		font-size: var(--text-body);
		font-weight: var(--weight-body);
		line-height: 1.5;
		max-width: 40ch;
		text-wrap: pretty;
		transition: color 0.3s ease;
	}

	.voice:hover {
		padding-left: calc(var(--i) * 2.7% + 1.1rem);
	}

	.voice:hover .entry {
		opacity: 1;
		transform: scaleY(1);
	}

	.voice:hover .name {
		font-variation-settings: 'wdth' 128, 'wght' 400;
	}

	.voice:hover .note {
		color: var(--color-paper-2);
	}

	@media (max-width: 780px) {
		.voice {
			grid-template-columns: 1fr;
			gap: var(--space-1);
			padding-left: calc(var(--i) * 2.4%);
		}

		.voice:hover {
			padding-left: calc(var(--i) * 2.4%);
		}

		.entry {
			left: calc(var(--i) * 2.4%);
		}
	}
</style>
